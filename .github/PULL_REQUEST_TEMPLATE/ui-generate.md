## 🎨 AI UI 생성

> ⚠️ **섹션명 = Scaffold 파일명** (정확히 일치해야 함!)
>
> 예: `ArticleCard.scaffold.svelte` → `### ArticleCard`

---

### 컴포넌트명

```svg

---

## 📝 변경 사항

<!-- 이 PR에서 변경된 내용을 설명해주세요 -->

---


## ✅ 체크리스트

- [ ] Scaffold 파일 생성 완료
- [ ] 디자인 SVG 코드 첨부 완료

---

## 📋 사용법

1. `ComponentName.scaffold.svelte` 파일 생성
2. Figma에서 프레임 선택 → **Copy as SVG** (Ctrl+Shift+C)
3. 위에 `### ComponentName` 섹션의 코드 블록에 붙여넣기
4. PR 생성 후 `ui-generate` 라벨 추가
5. AI가 `ComponentName.svelte` 자동 생성
6. (선택) `preview` 라벨로 Cloudflare 배포

---

## 💡 SVG 복사 방법

1. **Figma 열기** → 원하는 프레임/컴포넌트 선택
2. **우클릭** → Copy/Paste → **Copy as SVG**
3. 또는 단축키: `Ctrl+Shift+C` (Windows) / `Cmd+Shift+C` (Mac)
4. PR description의 코드 블록에 붙여넣기
```
